# Deutsch Learning Platform

Static GitHub Pages site for German learning notes.

## Build

```powershell
npm run build
```

The generated `index.html` is committed directly so GitHub Pages can serve the site from the repository root.

PDF files are ignored because GitHub blocks normal Git uploads above 100 MB. Keep large course books outside the repository or publish them through Git LFS/a separate storage location.
